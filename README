Installation:

1. clone git repo
2. `bundle install`
3. create config/initializers/devise_twitter.rb (go to dev.twitter.com to get your api keys):
>`Devise::Twitter.setup do |config|
>  config.consumer_key = ''
>  config.consumer_secret = ''
>  config.scope = :user
>end`

